Flask>=3.0,<4
openai>=1.0
supabase>=2.0
gunicorn>=22.0; platform_system != "Windows"
# Fusos horários (America/Sao_Paulo) para a virada de mês da fatura.
# No Linux costuma vir do sistema; no Windows é obrigatório.
tzdata

